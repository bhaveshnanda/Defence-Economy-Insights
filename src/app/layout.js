import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Defence & Economy Insights",
  description: "Explore insights on Defence Policy, Future Trends, Defence Tech, and Business Economy.",
  keywords: ["Defence", "Economy", "Technology", "Policy", "Next.js"],
  authors: [{ name: "Bhavesh Nanda" }],
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
